#include <bits/stdc++.h>
using namespace std;

int f(vector<long long> &nums){
    int n = nums.size();
    int l = 0, gcd = nums[0];
    
    

}

int main() {

    int n;
    cin>>n;

    vector<long long> nums(n);
    for(int i = 0; i < n; i++){
        cin>>nums[i];
    }

    cout<<f(nums);

    return 0;
}